apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: server-gateway
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 443
      name: https
      protocol: HTTPS
    tls:
      mode: SIMPLE
      credentialName: piripak-credential
    hosts:
    - charles.piripak.cc
    - teteche.piripak.cc